const masterColor = {
    color : 'light purple',
    backgroundColor: 'yellow',
    borderColor: 'red',
}

const styles = StyleSheet.create({
    rootScreen: {
      flex: 1
    },
    backgroundImage: {
      opacity: 0.25
    }
})

export default masterColor;